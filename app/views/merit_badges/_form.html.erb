<%= form_for(@merit_badge) do |f| %>
  <% form_error_messages(@merit_badge, "merit badge") %>
  <% form_text_field(f, :name) %>
  <% form_check_box(f, :eagle_required) %>
  <% form_text_field(f, :group_number) %>
  <% form_submit_buttons(f, use_save_and_new) %>
<% end %>

