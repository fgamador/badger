<h1><%= @outing.name %></h1>

<table class="detail">
  <tr>
    <th>Date:</th>
    <td><%= @outing.date %></td>
    <th>Nights of Camping:</th>
    <td><%= @outing.nights_of_camping %></td>
    <th>Long-Term Camp:</th>
    <td><%= @outing.long_term_camp %></td>
  <% if admin? %>
    <td class="actions"><%= link_to 'Edit', edit_outing_path(@outing) %></td>
<!--
    < % if @outing.unused? %>
      < %= link_to 'Delete', @outing,
        :confirm => "Delete unused #{@outing.name}?", :method => :delete %>
    < % end %>
-->
  <% end %>
  </tr>
</table>

